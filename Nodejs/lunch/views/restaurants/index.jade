extend ../layout

block content
  h2 Listing All Restaurants

  table
    tr
      th Name
      th Type
      th Phone
    each restaurant in restaurants
      tr
        td
          a(href='/restaurants/#{restaurant._id}')= restaurant.name
        td= restaurant.type
        td= restaurant.phone
